# PINN2026-1

To add submodule:

$git submodule init

$git submodule add git@github.com:YulieRu/PINN2026-1.git

To update (inside the submodule):

$git fetch

$git pull

To check to another branch:

$git checkout main

To update and init module (outcide the submodule in your repo):

$git submodule update --init


